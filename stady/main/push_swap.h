#ifndef PUSH_SWAP_H
#define PUSH_SWAP_H

#include <stdbool.h>  // bool 型を使うなら
#include <ctype.h>    // もともとの isdigit, isspace を使う場合
#include <stdio.h>    // printf を使う場合
#include <stdlib.h>   // malloc, free を使う場合

// 関数プロトタイプ
int ft_isdigit(int c);
int ft_isspace(int c);

#endif
